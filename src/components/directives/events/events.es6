app.component('eventsItem', {
    templateUrl: 'events.html',
    controllerAs: 'events',
    bindings: {
        img: '@',
        heading: '@'
    },
    controller: function ($element, $timeout) {

        var init = () => {};

        init();

        _.extend(this, {});
    }
});
