app.component('linksItem', {
    templateUrl: 'links.html',
    controllerAs: 'links',
    bindings: {
        img: '@',
        heading: '@'
    },
    controller: function ($element, $timeout) {

        var init = () => {};

        init();

        _.extend(this, {});
    }
});
