app.component('footItem', {
    templateUrl: 'foot.html',
    controllerAs: 'foot',
    bindings: {
        img: '@'
    },
    controller: function (Menu) {

        var init = () => {};

        init();

        _.extend(this, {
        });
    }
});
